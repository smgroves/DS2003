.DS_Store
.claude/
__pycache__/
~$*
Private/

# Answer-key / instructor-filled notebooks — never publish these publicly
*_filled_*.ipynb

# Too large for GitHub (100MB limit) — likely has embedded video; host separately if needed
Lectures/03-Why we visualize.pptx
